#include <stdio.h>

int main() {
    int n, i, soma = 0;

    printf("Digite um numero inteiro positivo: ");
    scanf("%d", &n);

    printf("Sequência de numeros de 1 a %d: ", n);
    for(i = 1; i <= n; i++) {
        printf("%d ", i);
        soma += i;
    }

    printf("\nSoma: %d\n", soma);

    return 0;
}
